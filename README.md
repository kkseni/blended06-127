🚀 Крок 1: Налаштування архітектури та API-сервісу. Не починайте з HTML чи CSS. Спочатку підготуйте «фундамент» — код, який буде спілкуватися з бекендом.Створіть модуль API (api.js або ApiService.js).Напишіть функції (або методи класу) для запитів:fetchCategories() — для отримання списку категорій.fetchProducts(category, page, limit) — універсальна функція для товарів. Вона має приймати категорію (за замовчуванням порожня) та номер сторінки (пагінація по 12).fetchProductById(id) — для детальної інформації про товар (для модалки).Протестуйте запити через console.log, щоб переконатися, що бекенд повертає правильні дані.

📦 Крок 2: Сервіс LocalStorage та стан додатка Оскільки кошик і список бажань зав'язані на локальне сховище, краще відразу винести цю логіку окремо.Створіть функції-хелпери: getStorage(key), setStorage(key, data).Сформуйте початковий стан (state): масиви для cart та wishlist, які при завантаженні сторінки зчитують дані з localStorage (або створюють порожні масиви []).

🏛️ Крок 3: Головна сторінка — категорії та перша порція товарівТепер переходимо до першого рендерингу на головній сторінці.Категорії: Викличте fetchCategories(), створіть HTML-рядок через map та вставте його в ul.categories.Товари (Перша сторінка): Викличте fetchProducts(null, 1, 12), згенеруйте картки та вставте в ul.products.Кнопка Load More:Створіть змінну let currentPage = 1.При кліку на кнопку збільшуйте сторінку currentPage++, робіть новий запит, а отримані товари додавайте (insertAdjacentHTML) в кінець списку.Якщо прийшло менше 12 товарів або бекенд каже, що це кінець — ховайте кнопку та викликайте
iziToast.info({ message: 'Це всі товари!' }).

🔀 Крок 4: Фільтрація за категоріями Повісьте слухач подій на ul.categories (використовуйте делегування подій).При кліку на категорію:Скидайте currentPage = 1.Очищайте список товарів ul.products.innerHTML = ''.Робіть запит fetchProducts(selectedCategory, 1, 12).Додавайте клас .active на клікнуту кнопку (і знімайте з попередньої).

🪟 Крок 5: Модальне вікно (Деталі товару)Це один із найскладніших етапів через роботу зі слухачами.Повісьте слухач на ul.products (знову делегування подій за ID картки).При кліку: робіть запит fetchProductById(id), відкривайте модалку та рендерить туди розмітку.Логіка закриття (важливо за ТЗ):При відкритті: додавайте клас is-open на модалку та вішайте window.addEventListener('keydown', onEscPress).При закритті: знімайте клас is-open та обов'язково робіть window.removeEventListener('keydown', onEscPress).

🛒 Крок 6: Логіка кнопок "Add to..." та оновлення інтерфейсуТепер пов'язуємо модалку з localStorage та навігацією.Оновлення лічильників: Напишіть функцію updateNavBadges(), яка рахує cart.length та wishlist.length і малює ці цифри в хедері. Викликайте її відразу при завантаженні сайту.При відкритті модалки: Перевірте, чи є id цього товару в масиві cart / wishlist. Якщо є — текст кнопки одразу має бути "Remove from...".При кліку на кнопку в модалці:Якщо товару немає в масиві — пушимо його туди, міняємо текст кнопки на "Remove...", оновлюємо localStorage та лічильник у хедері.Якщо є — видаляємо з масиву (через filter), міняємо текст на "Add to...", оновлюємо localStorage та лічильник.

📄 Крок 7: Сторінки Wishlist та Cart (Окремі HTML/JS файли)Коли головна сторінка повністю працює, переходьте до додаткових.Wishlist: Беремо масив із localStorage, рендеримо картки. Додаємо кнопку "Видалити" (при кліку видаляємо з локал сторедж і перерендеримо сторінку). Додаємо логіку модалки (можна перевикористати код з головної).Cart: Беремо масив із localStorage. Для кожної картки виводимо її ціну.Калькуляція: Загальна вартість рахується через метод масиву .reduce().Виводьте кількість, суму та малюйте кнопку "Оплатити" (при кліку на неї зазвичай очищають кошик і показують iziToast.success).

API ендпоінти:

https://dummyjson.com/docs/products - документація бекенду, розділ продукти
https://dummyjson.com/products?limit=10&skip=10 - отримати всі продукти з пагінацією
https://dummyjson.com/products/1 - отримати один продукт по ID
https://dummyjson.com/products/search?q=nail - пошук продукту по ключовому слову
https://dummyjson.com/products/category-list - отримати список категорій продуктів
https://dummyjson.com/products/category/smartphones - отримати продукти по категорії


